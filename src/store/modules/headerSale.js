const state = {
    sale: {
        price: '39'
    }
}

const getters = {
    salePrice: (state) => (state.sale.price)
}

const actions = {}

const mutations = {}

export default {
    state,
    getters,
    actions,
    mutations
}